textual>=0.58.0
onnxruntime==1.20.1
kokoro-onnx>=0.4.7
soundfile>=0.12.0
colorlog
# Note: Install kokoro-onnx with --no-deps, then install colorlog separately
# Or install onnxruntime first, then kokoro-onnx --no-deps, then colorlog

